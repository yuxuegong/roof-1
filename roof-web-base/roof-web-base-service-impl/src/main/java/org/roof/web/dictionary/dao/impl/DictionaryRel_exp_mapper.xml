<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.roof.web.dictionary.dao">
<!--  
	<sql id="columnsAs">
		t1.id, t1.classname, t1.tablename, t1.propertyname, t1.relid, t1.dicid
	</sql>

	<select id="selectDictionaryRelPage" resultType="org.roof.web.dictionary.entity.DictionaryRelVo">
		select
		<include refid="columnsAs"/>
		from
		s_dictionary_rel t1
		join
		(SELECT
		id
		from
		s_dictionary_rel
		where 1=1
		<if test="symbol != null and orderByVal != null">
			and id ${symbol} #{orderByVal}
		</if>
		<include refid="conds" />
		order by id ${orderByType}
		limit #{skipRow}, #{limit}) t2
		where t1.id = t2.id
	</select>
	
	<select id="selectDictionaryRelCount" resultType="java.lang.Long">
		select
		count(id)
		from s_dictionary_rel
		where 1=1
		<include refid="conds" />
	</select>
-->
</mapper>